# 興化語聖經數位化專案 (1912 Hinghua Bible Digital Project)

1912 年出版的興化語（莆仙語）聖經數位化網站專案。

## 專案簡介

本專案旨在建立一個優雅、易讀的網站，展示 1912 年出版的興化語聖經，保存珍貴的 19 世紀末至 20 世紀初莆仙語音韻和詞彙。

### 主要功能

- ✅ **多種閱讀模式**
  - 雙欄對照（羅馬字 | 漢字）
  - Ruby 注音（漢字上標羅馬字）
  - 單一語言（僅羅馬字或僅漢字）

- ✅ **原始掃描頁面檢視**
  - 點擊 📖 圖示查看對應的原始掃描頁面
  - 支援縮放、平移功能

- ✅ **全文搜尋**
  - 支援羅馬字和漢字搜尋
  - 即時顯示搜尋結果

- ✅ **響應式設計**
  - 適配桌面和行動裝置

## 技術架構

- **前端框架**: React + Vite
- **字體**: Tauhu-oo（興化語專用字體）
- **圖片格式**: WebP (1485 張掃描頁面，約 270MB)
- **部署**: GitHub Pages

## 本地開發

### 安裝依賴

```bash
cd website
npm install
```

### 啟動開發伺服器

```bash
npm run dev
```

開啟瀏覽器訪問 http://localhost:5173

### 構建生產版本

```bash
npm run build
```

構建產物會生成在 `website/dist/` 目錄。

## 專案結構

```
hinghua-singging/
├── .github/
│   └── workflows/
│       └── deploy.yml           # GitHub Pages 自動部署
├── website/                     # React 網站
│   ├── public/
│   │   ├── fonts/              # Tauhu-oo 字體檔案
│   │   ├── images/             # WebP 掃描頁面 (1485 張)
│   │   ├── bible_data.json     # 聖經文本資料
│   │   └── chapter-page-mapping.json  # 章節-頁碼對應
│   ├── src/
│   │   ├── components/         # React 組件
│   │   ├── App.jsx
│   │   ├── index.css
│   │   └── main.jsx
│   ├── index.html              # 主頁面
│   ├── viewer.html             # 圖片檢視器頁面
│   ├── package.json
│   └── vite.config.js
├── scripts/                     # Python 資料處理腳本
│   ├── parse_text_v3.py        # 文本解析器
│   ├── ocr_page_numbers.py     # OCR 頁碼識別
│   ├── regenerate_chapter_mapping.py  # 重新生成章節對應
│   └── book_info.py            # 書卷資訊
├── pics_webp/                   # WebP 圖片來源
├── hanci.txt                    # 漢字文本
├── lomaci.txt                   # 羅馬字文本
├── bible_data.json              # 解析後的聖經資料
├── chapter-page-mapping.json   # 章節-頁碼對應
└── README.md
```

## 資料處理

### 文本解析

```bash
python scripts/parse_text_v3.py
```

從 `lomaci.txt` 和 `hanci.txt` 解析文本，生成 `bible_data.json`。

### OCR 頁碼識別

```bash
python scripts/ocr_page_numbers.py
```

從掃描圖片中識別章節號碼，生成 `page-ocr-results.json` 和 `chapter-page-mapping.json`。

### 重新生成章節對應

```bash
python scripts/regenerate_chapter_mapping.py
```

從手動編輯的 `page-ocr-results.json` 重新生成 `chapter-page-mapping.json`。

## 部署

### 自動部署

推送到 `main` 分支會自動觸發 GitHub Actions，構建並部署到 GitHub Pages。

### 手動部署

```bash
cd website
npm run build
# 將 dist/ 目錄內容部署到 GitHub Pages
```

## 資料格式說明

### bible_data.json

```json
{
  "books": [
    {
      "name_han": "興化語聖經",
      "name_rom": "Hing-hua Bible",
      "chapters": [
        {
          "chapter": 1,
          "verses": [
            {
              "verse": 1,
              "rom": "Kî-táu Siō̤ng-Da̤̍ cho̤̍ng-cho̤̍ ting-dā̤.",
              "han": "起頭，上帝創造天地。",
              "tokens": [...]
            }
          ]
        }
      ]
    }
  ]
}
```

### Token 類型

- **single**: 1音節1字（如：有/ū）
- **compound**: 1音節多字，用「」標記（如：「家己」/ga̍i）
- **phrase**: 多音節多字（如：暝頭/má-láu）
- **punct**: 標點符號

## 授權

本專案為非商業性學術研究專案，旨在保存和推廣興化語文化遺產。

## 聯絡

如有問題或建議，歡迎開 Issue 討論。

---

**最後更新**: 2025-12-11
